# Terraform code 
This code will create a vpc, 3 public 3 private subnets in the VPC using VPC module.

The VPC will be passed to the EKS cluster to create the cluster inside the VPC



## Tools required
Terraform version 1.6.3


